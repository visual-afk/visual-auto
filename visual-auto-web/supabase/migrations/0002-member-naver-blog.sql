-- =====================================================================
-- 디자이너 개인 네이버 블로그 링크 (사람별 발행 대상)
-- 실행: Supabase 대시보드 SQL Editor 에 붙여넣기 (1회)
-- =====================================================================

-- 각 멤버가 자기 개인 네이버 블로그 글쓰기 주소를 등록 (발행 시 '네이버 블로그 열기' 대상)
alter table branch_users add column if not exists naver_blog_url text;
