-- =====================================================================
-- 지점 주소 컬럼: 주소 → 좌표 자동찾기(지오코딩)에 쓴 주소를 저장
-- 나중에 반경 조정·재검색이 쉬워짐
-- 실행: Supabase SQL Editor 에 붙여넣기
-- =====================================================================

alter table branches add column if not exists address text;
